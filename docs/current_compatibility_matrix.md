# SeLinOS current compatibility matrix

**Snapshot:** default production image `d3dbd31a2cb29c69c789b5796278a2ea1f437b32aada4da7bfdc95badcbf344e` on x86_64/PC99 QEMU TCG. The separate root-construction M1 proof image is `e0feaa890be16761c1fce88cf5bf005c701e3980d73ad1b6094bf8dcaf0b7184` with `SeLinRootConstructM1Adapter=ON`; the separate M2 provenance-cap proof image is `6ad24226570811f35d9e7ab4a7fd48cefe2d84f03faaa9dd36320b19b0023623` with both M1 and `SeLinRootConstructM2ControlLease=ON`; the separate M3 one-resume proof image is `a1ba3d41f41fc8b1d74477fa903444c017a8e0db58d7a9bfa4864f0887fecd59` with M1, M2 and `SeLinRootConstructM3SingleResume=ON`; the separate M4 completion-witness proof image is `d13119ed380447e16dbd0e8b88e05d1bdc675bb19f37a88c9d8b41ae80dbf71f` with M1–M3 and `SeLinRootConstructM4Completion=ON`; the separate root-only virtio M3 feature-stage proof image is `f0631b636069d0ce265475c73a87735ccadb5992b9662ac212538fa9c49fa3c5` with `SeLinRootVirtioBlkFeatureStageProbe=ON`; the separate root-only virtio M4 driver-ready/reset proof image is `94265456b94610fee20e4c8a3b70adce1a54d9ff06e03f9443b9e064ea6758f7` with `SeLinRootVirtioBlkDriverReadyProbe=ON`; the separate root-only virtio M5 queue-zero observation proof image is `eff3715282392d095b3559463e1e88c0db4616d70fdffdc353a4ca5dbd4dd638` with `SeLinRootVirtioBlkQueueZeroProbe=ON`; the separate root-only virtio M6 disabled queue-layout proof image is `1be04d607e073fed851a6047895c6186002a6587d8f239b91901aea0cabda718` with `SeLinRootVirtioBlkQueueLayoutProbe=ON`; the separate root-only virtio M7 queue-enable/read-back/reset proof image is `83d799fb3a81c92e523f1ba9362f73c79cf956408ee3a9ce957b549835b48ff8` with `SeLinRootVirtioBlkQueueEnableProbe=ON`; the separate root-only virtio M8 notification-observation proof image is `5b4540194ec688a74519889ee4f7d4ad2c7189702d7d0e3f2a30283e7fd1789e` with `SeLinRootVirtioBlkNotificationObservationProbe=ON`; the separate root-only virtio M9 zero-descriptor notification/reset proof image is `608fba9acccb24a274cba673f758e7be0f7e0f68a2dcda2ff35d3d5dafdd5717` with `SeLinRootVirtioBlkZeroDescriptorNotificationProbe=ON`; the separate root-only virtio M10 post-notification zero-index observation proof image is `c86989dc57e8cf4da71764db59189ea7a7f6d3bf316b8d733bc3fe9ad670ae37` with `SeLinRootVirtioBlkZeroIndexObservationProbe=ON`; and the separate Phase 43 x86_64 execute-disable proof image is `e7d7480d636b33b5ea1b27ea4cb4df42d52db8a465b3f2138d80ffca372240e4` with `SeLinX86NxMappingProbe=ON`. None is the default production profile. This matrix records only evidence-bound behavior. A green-looking narrow primitive is **not** a general Linux ABI, KAPI, driver or package-manager claim.

| Area | Verified, bounded behavior | Evidence | Explicitly not provided |
|---|---|---|---|
| seL4 isolation | Root plus independently created service/process domains with separate TCB/CSpace/VSpace topology; taskd M1 additionally owns the start, exact two-generation completion observation, one suspend/resume cycle and final success signal for one root-preprovisioned child TCB; objectd M1 owns a one-slot `FREE → RESERVED` state proof with exact duplicate refusal and no object allocation; memd M1 owns the analogous one-slot fixed-mapping-plan reservation proof with duplicate refusal and no VSpace/frame/mapping allocation; taskd reservation M1 coordinates one objectd and one memd status-only reservation pair, ending in `RESERVED_PENDING` with no lease capability, object, mapping or child created; opaque-lease M1 receives one root-preprovisioned notification token from objectd into a fixed taskd CSpace receive path, with no resource interpretation; TCB-lease M1 receives one child-TCB control-cap copy for a root-preprovisioned child that remains unspawned, and taskd provably does not exercise the cap; TCB-resume M2 receives a fresh child-TCB control cap, performs exactly one `seL4_TCB_Resume` and validates one fixed child completion; persistent-root-context M0 places root simple/VKA/VSpace descriptors under static root-private lifetime with a one-shot initialization guard; root-dispatch M0 handles one exact construction-status request on an endpoint sent only to its dedicated probe, returning no capabilities and constructing no child; in a separate opt-in M1 image, root retains the persistent VKA/loader VSpace, accepts one exact taskd-client request, constructs one root-selected inert child and spawns it suspended, returns only status, then rejects the client's duplicate request without another allocation; in the separate dual-gated M2 image, root transfers exactly one control-cap copy for that same constructed suspended child to a fixed taskd CSpace receive slot, and taskd proves non-exercise before its duplicate lease request is rejected with zero extra caps; in the separate triple-gated M3 image, taskd invokes exactly one `seL4_TCB_Resume` on that received cap and one root-selected child execution-witness marker follows; in the separate four-gated M4 image, root provides one fixed child/taskd completion-endpoint topology, the child sends one exact completion record after the one resume, and taskd validates it. | Existing M0–M3 boot and driver verifiers; `verify_taskd_fixed_child_lifecycle_m1.py`; `verify_objectd_fixed_inventory_m1.py`; `verify_memd_fixed_mapping_inventory_m1.py`; `verify_taskd_combined_reservation_m1.py`; `verify_opaque_lease_cap_transfer_m1.py`; `verify_tcb_control_lease_m1.py`; `verify_tcb_resume_m2.py`; `verify_persistent_root_context_m0.py`; `verify_root_dispatch_m0.py`; `verify_root_construct_m1.py`; `verify_root_construct_m2.py`; `verify_root_construct_m3.py`; `verify_root_construct_m4.py`; `phase22_clone_task_lifecycle_gate.md`. | Hardware IOMMU/VFIO-like containment; general or dynamic root child construction, general child-capability delivery or rights model, child register/stack/TLS setup, general IPC or a second/any other lifecycle action, Linux clone/fork/pthread, exit/teardown/reuse, actual resource lease, object/capability allocation, VSpace/frame/mapping allocation, dynamic task creation, parent/child state, shared-resource semantics and full scheduler semantics. |
| PCI/EDU driver path | QEMU EDU enumeration; token-validated deviced, IRQ and DMA lifecycle; dual-device proof. | `verify_driver_runtime_m1.py`, `verify_m3_two_edu.py`. | General PCI bus manager, hotplug, arbitrary driver support or IOMMU. |
| Root PCI allocation | 32-bit BAR sizing/program/read-back only in isolated e1000 fixture. | PCI allocator gate documentation. | General resource allocator, BAR delegation or NIC/storage mapping. |
| Linux KAPI | Bounded `pci_register_driver`, token-bound EDU IRQ/DMA flow, completion/work/timer/spinlock slice; M2 proves canonical `ioremap` fails closed before device enable and `iounmap(NULL)` is a no-op. | KAPI relocation/fixture, `verify_kapi_sync_m1.py` and `verify_kapi_m2_ioremap_failclosed.py`. | Device frame/MMIO mapping, cache policy, canonical IRQ stub or new IRQ authority, scheduler workers, timers, SMP locking, broad Linux internal API/KABI. |
| Linux module/KABI | Linux 6.18.44 fixture parser, relocation/integrity policy; controlled fixture evidence. | KABI verifiers. | Native untrusted `.ko` execution, general symbol surface or production module loader. |
| Sealed static-image grammar prerequisite | Separate default-OFF Phase 64 M0 profile parses one deterministic 4 KiB SSIM-v1 byte stream with one RX segment, fixed low-canonical entry, zeroed-digest SHA-256 source seal and payload-digest ledger. It rejects altered digest, W+X, non-canonical entry, reserved-field and range inputs, then rejects a duplicate request; the transaction is status-only. | `verify_sealed_static_image_m0.py`; `selinos_sealed_static_image_m0.verification.json`; `phase64_sealed_static_image_m0_gate.md`; QEMU TCG profile image `ee7a5d0df167fc2a86a8bf82c517a415dc44e0737e45b675b00ccaa98d7a3bed`. | Image storage, capability transfer, frame allocation, mapping, W^X transition, task resume, instruction fetch, ELF, relocation, C runtime, process lifecycle, Linux ABI, `dpkg`, or `apt`. |
| One terminal target capability deletion transaction | Separate default-OFF Phase 70 M0 profile reaches the terminal invalid-opcode UserException, completes Phase 69 entry-stack-IPC target mapping revocation, then calls `seL4_CNode_Delete` successfully for exactly six **target-CNode** copies: notification slot 1, IPC-frame slot 2, fault-endpoint slot 3, entry-frame slot 4, stack-frame slot 5 and self-CNode slot 0 last. Root descriptors and all target kernel objects remain retained; there is no reply, resume, root-cap deletion, object free, ASID action or successor construction. | `verify_static_image_target_capability_deletion_m0.py`; `selinos_static_image_target_capability_deletion_m0.verification.json`; `phase70_static_image_target_capability_deletion_m0_gate.md`; isolated QEMU TCG profile `build-static-image-target-capability-deletion-probe`. | Global revocation, root capability deletion, object destruction, frame/page-table reclamation, ASID or task reuse, process exit/creation, ELF, Linux ABI, `dpkg`, or `apt`. |
| One terminal target mapping revocation transaction | Separate default-OFF Phase 69 M0 profile reaches the already established terminal invalid-opcode UserException, then calls `seL4_X86_Page_Unmap` successfully for exactly the target entry (`0x60000000`), stack (`0x70002000`) and IPC (`0x70000000`) frame caps in that order. The root retains the target frames, TCB, CNode, PML4, endpoint and paging objects; it does not reply, resume, delete capabilities, free objects, recycle ASIDs or construct a successor. The witness does not independently inspect page-table entries. | `verify_static_image_mapping_revocation_m0.py`; `selinos_static_image_mapping_revocation_m0.verification.json`; `phase69_static_image_mapping_revocation_m0_gate.md`; isolated QEMU TCG profile `build-static-image-mapping-revocation-probe`. | General VSpace teardown, independent PTE readback, page-table/frame/capability reclamation, ASID or task reuse, process exit/creation, ELF, Linux ABI, `dpkg`, or `apt`. |
| One VM-fault repair/restart prerequisite | Separate default-OFF Phase 63 M0 profile initializes one `nop; ud2` entry frame but leaves it unmapped; one explicit resume generates one badged instruction-fetch VM fault. Root maps that exact frame executable, sends one zero-label reply, then observes terminal `ud2` UserException at `rip=0x60000001` and withholds reply. | `verify_taskd_vm_restart_m0.py`; `selinos_taskd_vm_restart_m0.verification.json`; `phase63_reply_terminal_fault_m0_gate.md`; QEMU TCG profile image `050afea3e6c7e23607aad2d937d343094da177c9095b4c4743aa8d0939588993`. | UserException reply, fault-register mutation, second reply/resume, generic continuation, exit lifecycle, ELF/C runtime, arbitrary code execution, general process launch, Linux ABI, `dpkg`, or `apt`. |
| One-NOP executable instruction-fetch prerequisite | Separate default-OFF Phase 62 M0 profile writes exactly `nop; ud2` into one root-only entry-frame alias, unmaps that alias, maps the entry page executable, keeps the stack NX, and resumes exactly once. Root receives one badged x86 user exception at `rip=0x60000001`, `rsp=0x70002ff8`, vector 6, then withholds reply. | `verify_taskd_exec_fetch_m0.py`; `selinos_taskd_exec_fetch_m0.verification.json`; `phase62_executable_fetch_witness_m0_gate.md`; QEMU TCG profile image `36c85b47fca7042bc6aa9bf9f469afcbdca7eec17cc01c8f3517ceedc123c5a9`. | Any instruction beyond the one NOP witness, target continuation, fault reply, a second resume, executable stack, initial stack image, ELF/C runtime, arbitrary code execution, general process launch, Linux ABI, `dpkg`, or `apt`. |
| Entry-point and stack provenance prerequisite | Separate default-OFF Phase 61 M0 profile adds distinct entry and stack frames at target CSpace slots 4 and 5. Root maps both with x86 `ExecuteDisable`, requests and reads back `rip=0x60000000` and ABI-derived `rsp=0x70002ff8` (`rsp mod 16 = 8`), and applies canonical-address and misalignment negative guards. The target remains suspended. | `verify_taskd_entry_stack_m0.py`; `selinos_taskd_entry_stack_m0.verification.json`; `phase61_entry_stack_provenance_m0_gate.md`; QEMU TCG profile image `1393d044eb49a879fb0a38ac12fd3464d9c92882a5948fdd04d4a6663c326dd3`. | Executable mapping, instruction bytes/fetch/completion, target resume, fault delivery/reply, return address, initial stack image, ELF runtime, general process launch, clone/fork/pthread, DMA containment, persistent storage, networking, `dpkg`, `apt`, or general Linux compatibility. |
| Fault-mediated first-execution dispatch prerequisite | Separate default-OFF Phase 60 M0 profile adds a badged target fault-endpoint capability at CSpace slot 3 to the Phase 59 topology. Root writes/reads the requested-zero 20-word context, performs **exactly one** target resume, receives one badged x86_64 instruction-fetch VM fault with IP/address zero, then deliberately withholds fault reply; target remains fault-blocked. | `verify_taskd_fault_witness_m0.py`; `selinos_taskd_fault_witness_m0.verification.json`; `phase60_fault_mediated_first_execution_m0_gate.md`; QEMU TCG profile image `957378c26defa3fd9722698d1a7af857f3b55e755e073642e4fdadc39db1a4c4`. | Successful instruction completion, entry point, stack, TLS base, fault repair, fault reply, a second resume, ELF runtime, general process launch, dynamic lifecycle, clone/fork/pthread, DMA containment, persistent storage, networking, `dpkg`, `apt`, or general Linux compatibility. |
| Requested-zero register context prerequisite | Separate default-OFF Phase 59 M0 profile reproduces the Phase 58 mapped IPC-buffer topology, writes all 20 x86_64 `seL4_UserContext` words as zero with `resume_target=0`, and reads all words back before moving TCB/CNode/PML4 caps to taskd. Pinned seL4 x86 sanitization deterministically returns only word 2 (`rflags`) as user-safe `0x202`; the other 19 words are zero. A one-owned/one-rejected status transaction is witnessed. | `verify_taskd_zeroed_context_m0.py`; `selinos_taskd_zeroed_context_m0.verification.json`; `phase59_zeroed_register_context_m0_gate.md`; QEMU TCG profile image `90724ead3a664f48c2ca9f33e95974b837e36ba08259776843b25d519c6acd51`. | Entry point, stack, TLS base, target invocation, resume, instruction execution, ELF runtime, fault handler, dynamic process lifecycle, clone/fork/pthread, DMA containment, persistent storage, networking, `dpkg`, `apt`, or general Linux compatibility. |
| Isolated IPC-buffer mapped suspended-task prerequisite | Separate default-OFF Phase 58 M0 profile allocates and rolls back a generation-1 TCB/CNode/PML4/frame bundle, then maps one ordinary 4 KiB frame at fixed, 512-byte-aligned target VA `0x70000000` through a generation-2 ASID-assigned PML4. It copies the frame capability into self-rooted target CSpace slot 2 and configures that address/frame as the target IPC buffer before moving TCB/CNode/PML4 caps to taskd. A one-owned/one-rejected status transaction is witnessed; the target has no registers, endpoint invocation, stack, ELF image, or resume. | `verify_taskd_ipc_buffer_m0.py`; `selinos_taskd_ipc_buffer_m0.verification.json`; `phase58_ipc_buffer_mapping_m0_gate.md`; QEMU TCG profile image `bf399c8dd51fa4b8a7be7c9fb13ab70f1abc5615825f26e756eb76a5c4b4607a`. | IPC activity, target invocation, register context, resume, execution, stack, ELF/W^X loading, fault handling, dynamic process lifecycle, clone/fork/pthread, DMA containment, persistent storage, networking, `dpkg`, `apt`, or general Linux compatibility. |
| Self-rooted two-cap CSpace prerequisite | Separate default-OFF Phase 57 M0 profile inserts exactly two caps into final target CNode: self-CNode at slot 0 and notification at slot 1, both at four-bit radix depth, before ASID assignment and TCB configuration. TCB/CNode/PML4 move to taskd and remain non-executing. | `verify_taskd_self_rooted_cspace_m0.py`; `selinos_taskd_self_rooted_cspace_m0.verification.json`; `phase57_self_rooted_cspace_suspended_task_m0_gate.md`. | IPC buffer, target invocation, registers, resume, mapping, ELF runtime, process lifecycle, clone/fork/pthread or package compatibility. |
| One-cap populated CSpace prerequisite | Separate default-OFF Phase 56 M0 profile root-copies exactly one notification cap into slot 1 of a final 16-slot CNode at the CNode’s four-bit radix depth, after a generation-1 rollback and before generation-2 PML4 ASID assignment and `seL4_TCB_Configure`. The configured TCB/CNode/PML4 caps then move—not copy—to taskd slots 9–11; taskd witnesses one status query then duplicate rejection. Target notification, registers and TCB remain uninvoked and non-executing. | `verify_taskd_populated_cspace_m0.py`; `selinos_taskd_populated_cspace_m0.verification.json`; `phase56_populated_cspace_suspended_task_m0_gate.md`. | Initial CSpace ABI, any second target cap, IPC buffer, target notification invocation, fault handler, scheduler/process lifecycle, post-configuration cleanup/reclaim, paging/frame lifecycle, mappings, W^X, ELF runtime, execution, `clone`, `fork`, `vfork`, `pthread`, package compatibility. |
| Configured but non-executing task linkage prerequisite | Separate default-OFF Phase 55 M0 profile root-allocates generation-2 TCB/CNode/PML4 after generation-1 pre-configuration rollback, assigns the PML4 an ASID, calls `seL4_TCB_Configure` to associate the TCB with its CSpace/VSpace, then moves—not copies—the configured caps to fixed taskd slots 9–11. Taskd returns exactly one ownership witness and rejects the duplicate; the TCB receives no registers, priority, scheduling or resume and remains non-executing. | `verify_taskd_suspended_linkage_m0.py`; `selinos_taskd_suspended_linkage_m0.verification.json`; `phase55_suspended_task_linkage_m0_gate.md`. | Post-configuration rollback/reclaim, ASID recycle, CNode population, IPC buffer/fault handler, intermediate paging/frame lifecycle, mappings, W^X, ELF loading, scheduler/process lifecycle, execution, `clone`, `fork`, `vfork`, `pthread`, process ABI or package compatibility. |
| Inert dynamic task-resource bundle rollback and ownership prerequisite | Separate default-OFF Phase 54 M0 profile root-allocates one TCB, one small CNode and one x86_64 PML4, deliberately frees all generation-1 objects in reverse order before delegation, then allocates generation 2 and moves—not copies—its three caps to fixed taskd CSpace slots 9–11. Taskd returns exactly one ownership witness and rejects the duplicate; all final resources remain mutually unlinked, inert and unexecuted. | `verify_taskd_inert_bundle_m0.py`; `selinos_taskd_inert_bundle_m0.verification.json`; `phase54_inert_task_resource_bundle_m0_gate.md`. | Task construction, TCB CSpace/VSpace linkage, ASID, CNode population, paging/frame lifecycle, mappings, W^X, ELF runtime, execution, `clone`, `fork`, `vfork`, `pthread`, process ABI or package compatibility. |
| Dynamic VSpace-root rollback and ownership prerequisite | Separate default-OFF Phase 53 M0 profile root-allocates an x86_64 PML4 via `vka_alloc_vspace_root`, deliberately rolls back generation 1 with `vka_free_object` before delegation, then allocates generation 2 and moves—not copies—its cap to fixed taskd CSpace slot 9. Taskd returns exactly one generation-2 ownership witness and rejects the duplicate query; the final PML4 remains inert, unassigned and unmapped. | `verify_taskd_dynamic_vspace_m0.py`; `selinos_taskd_dynamic_vspace_m0.verification.json`; `phase53_dynamic_vspace_root_rollback_m0_gate.md`. | ASID assignment, active VSpace layout, intermediate paging objects, frame lifecycle, mapping, W^X, target TCB linkage, execution, `clone`, `fork`, `vfork`, `pthread`, process ABI or ELF runtime. |
| Dynamic CNode rollback and ownership prerequisite | Separate default-OFF Phase 52 M0 profile root-allocates a small CNode, deliberately rolls back generation 1 via `vka_free_object` before any delegation, then allocates generation 2 and moves—not copies—its cap to fixed taskd CSpace slot 9. Taskd returns exactly one generation-2 ownership witness and rejects the duplicate query; the final CNode stays empty, inert and is not a TCB CSpace root. | `verify_taskd_dynamic_cspace_m0.py`; `selinos_taskd_dynamic_cspace_m0.verification.json`; `phase52_dynamic_cspace_rollback_m0_gate.md`. | Task CSpace construction, CNode population/rights/revocation, post-delegation rollback or reclaim, CSpace reuse, target TCB linkage, VSpace allocation, task execution, `clone`, `fork`, `vfork`, `pthread`, process ABI or ELF runtime. |
| Dynamic TCB ownership prerequisite | Separate default-OFF Phase 51 M0 profile allocates one inert target `seL4_TCBObject` via root VKA and moves—not copies—its cap to fixed taskd CSpace slot 9. Taskd returns exactly one ownership-location witness and rejects the duplicate query; the target is never configured, mapped, resumed or invoked. | `verify_taskd_dynamic_tcb_m0.py`; `selinos_taskd_dynamic_tcb_m0.verification.json`; `phase51_dynamic_tcb_ownership_m0_gate.md`. | Dynamic CSpace/VSpace construction, target destruction/reclamation or post-allocation rollback, target execution, scheduler/process/thread lifecycle, `clone`, `fork`, `vfork`, `pthread`, PID/TID, TLS, signals, wait/reap, ELF runtime. |
| Dynamic task allocation prerequisite | Separate default-OFF Phase 50 M0 profile runs one taskd-local status-only slot transition: reserve generation 1, duplicate reject, release generation 1, then re-reserve generation 2. It creates no TCB, CSpace, VSpace, executable image or process. | `verify_taskd_dynamic_alloc_m0.py`; `selinos_taskd_dynamic_alloc_m0.verification.json`; `phase50_dynamic_task_allocation_prerequisite_m0_gate.md`. | Dynamic object construction, capability ownership/rollback, thread execution, `clone`, `fork`, `vfork`, `pthread`, PID/TID, TLS, signals, wait/reap, process ABI, ELF runtime. |
| Linux syscall ABI | Fault-mediated `getpid`, bounded write, one-page mmap/brk, exact-literal `/selinos-release` ROMFS bridge and identity/TID M5; M6 fixed `clock_gettime(CLOCK_MONOTONIC)`, fixed `getrlimit(RLIMIT_NOFILE)`, immediate mismatched `FUTEX_WAIT → -EAGAIN` and zero-result `FUTEX_WAKE`; M7 fixed volatile-VFS lifecycle; M8 acknowledges only `mprotect` on the existing RW test page, `lseek(fd=5,0,SEEK_SET)` as a stateless no-op, and `pread64(fd=5,count=8,offset=0)` through the same volatile VFS read; M10 returns only a declared two-word probe record for `fstat(fd=6)`; M11 rejects only a fixed `getdents64(fd=6, count=32)` shape with `-ENOTDIR` and performs no buffer copy or VFS IPC; M12 root-mediates only `arch_prctl(ARCH_SET_FS, mapped_test_page)` through retained probe-TCB authority and verifies a fixed `%fs:0` store/load; M13 returns only that root-tracked post-M12 mapped base through `ARCH_GET_FS` to a validated user word; M14 returns one fixed 390-byte x86_64 utsname-layout record on the mapped test page; M15 accepts only `fcntl(fd=6,F_GETFL,0)` and returns exactly `0` for the one immutable read-only record, with no VFS IPC, client-memory mapping or state mutation. | syscall M1–M6 verifiers; `verify_linux_vfs_volatile_bridge_m7.py`; `verify_linux_syscall_abi_m8.py`; `verify_linux_fstat_record_m10.py`; `verify_linux_getdents_rejection_m11.py`; `verify_linux_tls_arch_prctl_m12.py`; `verify_linux_tls_arch_get_fs_m13.py`; `verify_linux_uname_m14.py`; `verify_linux_fcntl_getfl_m15.py`. | Linux `struct stat`/`statx`, directory FDs/cursors/`linux_dirent64`, general mprotect/W^X semantics, unmap/remap, VM permissions, general `fcntl` commands and mutable descriptor/file-status flags, descriptor/metadata/cursor/offset semantics, arbitrary/partial pread, host/UTS namespace semantics, general `ARCH_GET_FS`, GS base, arbitrary TLS, clone TLS inheritance, ELF TLS/dynamic linker, blocking futex/wait queues, timeouts, signals, general glibc ABI or process lifecycle. |
| Immutable and volatile VFS | CPIO `newc` parser, three known immutable files, fixed FD lifecycle and 16-byte packed pathname proof; plus one isolated server-owned **volatile** `/selinos-state` word. M7 bridges exact Linux `/selinos-state` open/read/fixed-word-write/read-back/close; M9 additionally bridges only exact immutable `/selinos-version` `openat → FD 6`, 8-byte `read → SELINOS9`, `close`. | ROMFS M1, bridge M2, CPIO M1, packed-path M1, `verify_vfs_volatile_m0.py`, `verify_linux_vfs_volatile_bridge_m7.py` and `verify_linux_vfs_version_bridge_m9.py`. | Persistent writes, directories, symlinks, metadata, arbitrary paths, dynamic FDs, stream cursor/offset semantics, multi-page I/O, multi-client ownership or POSIX filesystem semantics. |
| x86_64 execution permissions | In a separate default-OFF Phase 43 profile only, the local x86 execute-disable VM-attribute bit occupies bit 3 without changing cache enum values; the x86_64 4 KiB, 2 MiB and 1 GiB leaf constructors propagate it to `xd`, while x86_32 fails closed before mapping. A fresh one-byte `ret` page is mapped execute-disabled at fixed child VA `0x70000000`, produces one instruction VMFault, is unmapped/remapped executable after the temporary root write alias is removed, and returns one fixed success witness. | `verify_x86_nx_mapping_m0.py`; `selinos_x86_nx_mapping_m0.verification.json`; `phase43_x86_64_nx_mapping_extension_gate.md`. | General executable mappings, ELF loader policy, `PT_LOAD`, relocation, dynamic linking/TLS, process launch, native `.ko` execution, hardware validation, Linux runtime compatibility, `dpkg` or `apt`. |
| ELF runtime | ELF64/x86_64 metadata parser and W^X policy gate; M2 execution remains explicitly non-executing. Phase 43 removes only the isolated fixed-page NX/execute-control blocker and is not yet integrated with loader ownership, segment policy or runtime teardown. | `verify_elfrt_m1.py`; `verify_x86_nx_mapping_m0.py`; `phase21_elfrt_m2_wx_blocker_checkpoint_2026-08-14.md`. | PT_LOAD mapping, relocation application, `PT_INTERP`, constructors, dynamic linker or executable launch. |
| Storage | Separate default-OFF M0/M1/M2 QEMU proofs: identity discovery; common-capability metadata; then root-only 32/64-bit BAR size/bounds validation with exact restore, one temporary **uncached read-only** common-config page and the current selected feature window observed. A separate root-only M3 proof performs one zero-driver-feature `0 → ACKNOWLEDGE → DRIVER → FEATURES_OK → 0` status stage with read-back acceptance and temporary mapping teardown; M4 extends it with one `DRIVER_OK` read-back and immediate reset; M5 writes only `queue_select=0`, observes one non-zero queue size, then resets. In the separate M6 profile, root allocates one ordinary temporary 4 KiB frame, zero-fills a one-slot split-ring layout, writes/reads back `queue_size=1` and the three physical address pairs, while `queue_enable` is read as zero before and after and is never written; the device then resets and every mapping, reservation and frame is released. In separate M7, the same unpublished zeroed layout is used to write/read back `queue_enable=1` once and then to observe `queue_enable=0` after immediate device-status reset. In separate M8, root parses one non-zero-multiplier notification capability, validates scalar BAR bounds, reads queue-zero `queue_notify_off` through the temporary common page and calculates one notification physical address inside that capability window; the notification BAR is never framed, mapped, read or written. In separate M9, root creates a fresh zeroed one-slot layout, writes queue-enable once, maps only the calculated notification page and writes one zero-valued queue-zero notification, then immediately resets and tears all mappings down. In separate M10, root additionally samples avail flags/index and used flags/index immediately before and after the one notification; all eight samples are zero in the named QEMU trace. The disposable raw fixture remains unchanged; M6–M10 make no DMA or containment claim. | `verify_virtio_blk_discovery_m0.py`; `verify_virtio_blk_common_cap_m1.py`; `verify_virtio_blk_bar_feature_m2.py`; `verify_virtio_blk_feature_stage_m3.py`; `verify_virtio_blk_driver_ready_m4.py`; `verify_virtio_blk_queue_zero_m5.py`; `verify_virtio_blk_queue_layout_m6.py`; `verify_virtio_blk_queue_enable_m7.py`; `verify_virtio_blk_notification_observation_m8.py`; `verify_virtio_blk_zero_descriptor_notification_m9.py`; `verify_virtio_blk_zero_index_observation_m10.py`. | Driver BAR/frame-cap delegation; feature selector/driver-feature writes, complete feature enumeration, usable or safe device activation, descriptor publication or consumption, retained queue state, completion semantics, IRQ, PCI bus-mastering writes, DMA behavior/containment, IOMMU/IOSpace policy, block reads/writes, storage server and persistent filesystem. |
| Network | Separate default-OFF QEMU proofs: N0 root-only read-only e1000 PCI identity `8086:100e`; N1 exact-match identity, temporarily quiesce memory/master command bits, size then exactly restore/read-back a firmware-assigned 32-bit BAR0, and return scalar range metadata only. | `verify_network_e1000_discovery_n0.py`; `verify_network_e1000_bar_range_n1.py`. | BAR frame delegation/mapping, a driver domain, post-restore bus mastering, IRQ/DMA, frames, IP, DNS, TCP, HTTP(S), IOMMU/IOSpace containment, time or repository trust. |
| IOMMU containment precondition | Separate default-OFF Phase 49 M0 QEMU profile reads only `seL4_BootInfo.numIOPTLevels`; it observed `0`, which is a reproducible blocker for future containment work in this fixture. Root neither creates IOSpace nor modifies PCI/device state. Image: `c64df19583f38d8c6a747f71f63cb0bcc26554359f86c70f65fc15d426959984`. | `verify_iommu_availability_m0.py`; `selinos_iommu_availability_m0.verification.json`; `phase49_x86_iommu_availability_m0_gate.md`. | IOMMU configuration, requester attachment, translation tables, mapping/revocation, fault handling, DMA behavior or containment, device authority, block I/O, persistent VFS. |
| Debian packages | None. | `dpkg_apt_prerequisite_gate.md`. | `dpkg`, `apt`, `.deb` installation, maintainer scripts, repository acquisition and package state database. |

> **Interpretation rule:** any row marked “verified” has only the behavior described in that cell. Future claims must name the image/profile, add a dedicated independent verifier and retain all excluded semantics until separately proven.

## Active architectural blockers

| Blocker | Consequence | Required resolution evidence |
|---|---|---|
| NX extension is a local default-OFF fixed-page proof, not a loader/runtime policy | No production native ELF, dynamic loader or `.ko` execution claim; Phase 43 proves only one fixed fault/remap/control sequence in QEMU TCG. | Integrate a reviewed x86_64 execute-permission API with W^X-safe `PT_LOAD` ownership, relocation/dynamic-linker lifetime and execution/teardown tests. |
| QEMU TCG reports `ACPI: 0 IOMMUs detected` despite `CONFIG_IOMMU=1` | No hardware-DMA containment evidence in the current development profile. | Target with detected VT-d plus IOSpace mint/map/unmap/invalidation verifier. |
| No virtio/e1000 resource delegation beyond root experiments | No storage or network domain. Storage M2 releases its temporary device frame before return; storage M3–M5 reset their temporary root-only state to zero and release mapping/frame; network N1 returns scalar BAR metadata only after exact PCI restoration. | Exact-device feature-policy, queue, IRQ/DMA and teardown verifier with a detected IOMMU. |
| No writable persistent VFS | No package database or package extraction. | Crash-consistent storage/filesystem transaction tests. |
| No dynamic TLS/thread lifecycle | M12/M13 provide only the pinned isolated-probe FS-base proof, and taskd M1 controls only one preprovisioned child lifecycle. There is still no dynamic task construction, clone flag policy, stack/register setup, TID/futex exit or teardown. | Audited taskd allocator/CSpace/VSpace ownership, child register/stack policy, clone/futex lifecycle and teardown model. |

## Evidence commands

```bash
cd /home/ubuntu/helixos
./tools/verify_driver_runtime_m1.py
./tools/verify_m3_two_edu.py
./tools/verify_kabi_relocation_stage.py
./tools/verify_kabi_fixture_execution.py
./tools/verify_linux_syscall_abi_m1.py
./tools/verify_linux_syscall_abi_m2.py
./tools/verify_linux_syscall_abi_m3.py
./tools/verify_linux_syscall_abi_m4.py
./tools/verify_linux_syscall_abi_m5.py
./tools/verify_linux_syscall_abi_m6.py
./tools/verify_linux_vfs_volatile_bridge_m7.py
./tools/verify_linux_syscall_abi_m8.py
./tools/verify_linux_fstat_record_m10.py
./tools/verify_linux_getdents_rejection_m11.py
./tools/verify_linux_tls_arch_prctl_m12.py
./tools/verify_linux_tls_arch_get_fs_m13.py
./tools/verify_linux_uname_m14.py
./tools/verify_linux_fcntl_getfl_m15.py
./tools/verify_linux_vfs_version_bridge_m9.py
./tools/verify_taskd_fixed_child_lifecycle_m1.py
./tools/verify_objectd_fixed_inventory_m1.py
./tools/verify_memd_fixed_mapping_inventory_m1.py
./tools/verify_taskd_combined_reservation_m1.py
./tools/verify_opaque_lease_cap_transfer_m1.py
./tools/verify_tcb_control_lease_m1.py
./tools/verify_tcb_resume_m2.py
./tools/verify_persistent_root_context_m0.py
./tools/verify_root_dispatch_m0.py
./tools/verify_root_construct_m1.py
./tools/verify_root_construct_m2.py
./tools/verify_root_construct_m3.py
./tools/verify_root_construct_m4.py
./tools/verify_romfs_m1.py
./tools/verify_romfs_syscall_bridge_m2.py
./tools/verify_romfs_cpio_m1.py
./tools/verify_romfs_packed_path_m1.py
./tools/verify_elfrt_m1.py
./tools/verify_kapi_sync_m1.py
./tools/verify_kapi_m2_ioremap_failclosed.py
./tools/verify_virtio_blk_discovery_m0.py
./tools/verify_virtio_blk_common_cap_m1.py
./tools/verify_virtio_blk_bar_feature_m2.py
./tools/verify_virtio_blk_feature_stage_m3.py
./tools/verify_virtio_blk_driver_ready_m4.py
./tools/verify_virtio_blk_queue_zero_m5.py
./tools/verify_virtio_blk_queue_layout_m6.py
./tools/verify_virtio_blk_queue_enable_m7.py
./tools/verify_virtio_blk_notification_observation_m8.py
./tools/verify_virtio_blk_zero_descriptor_notification_m9.py
./tools/verify_virtio_blk_zero_index_observation_m10.py
./tools/verify_network_e1000_discovery_n0.py
./tools/verify_network_e1000_bar_range_n1.py
./tools/verify_vfs_volatile_m0.py
./tools/verify_x86_nx_mapping_m0.py
./tools/verify_static_image_mapping_revocation_m0.py
./tools/verify_static_image_target_capability_deletion_m0.py
```
