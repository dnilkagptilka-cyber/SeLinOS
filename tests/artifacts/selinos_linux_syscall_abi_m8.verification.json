{
  "schema": 1,
  "milestone": "SeLinOS Linux syscall ABI M8 bounded mprotect/lseek/pread64 proof",
  "platform": {
    "architecture": "x86_64 PC99",
    "emulator": "QEMU TCG instruction-count mode",
    "linux_kernel_present": false
  },
  "image": {
    "path": "build/images/selinos-root-image-x86_64-pc99",
    "sha256": "06637758b91049770906f412d0dd3d8e7fd3cedf1242419af69b44939a8e4ec0"
  },
  "implementation": {
    "root_dispatcher": {
      "path": "src/projects/helixos/src/domain_manager.c",
      "sha256": "52c208532a23f8c641d3e2a9357b68ca52d40254db045bd7ea99ba2f703ce124"
    },
    "isolated_probe": {
      "path": "src/projects/helixos/drivers/linux_syscall_probe.c",
      "sha256": "f0f7f78dbd6b91d11aaf82dd2fb628a0ef62f02b5384a5c64722d000f1a4c177"
    }
  },
  "contract": {
    "mprotect": "Accept only mprotect(SELINOS_LINUX_MMAP_ADDRESS, 4096, PROT_READ|PROT_WRITE) for the existing isolated mapped page; return zero as an acknowledgement without changing mapping permissions.",
    "lseek": "Accept only lseek(fd=5, offset=0, SEEK_SET) and return zero; it is a stateless no-op because the server has no cursor state.",
    "pread64": "Accept only pread64(fd=5, mapped one-page buffer, count=8, offset=0); root reads existing volatile state through ROMFS IPC, copies exactly one word and returns eight.",
    "negative_scope": "No arbitrary mprotect, page permission transition, unmap/remap, seek cursor, partial I/O, nonzero offset, immutable-FD pread or general file descriptor semantics are provided."
  },
  "runtime_evidence": {
    "path": "tests/artifacts/selinos_linux_syscall_abi_m8.boot.log",
    "sha256": "537339e4ce0bc765c1cdb9cc72f4b85bc609f4b649908bc3cc81b9a56c886396",
    "required_markers": [
      "SeLinOS ABI M8: mapped-page mprotect acknowledgement, zero-origin lseek and fixed-offset volatile pread64 mediated.",
      "SeLinOS ABI M7: fixed volatile VFS open/read/write/read-back/close bridge mediated.",
      "SeLinOS ABI M6: deterministic clock_gettime/getrlimit and non-blocking futex mismatch/wake mediated."
    ],
    "forbidden_markers": [
      "SeLinOS ABI gateway: bounded mprotect rejected.",
      "SeLinOS ABI gateway: bounded lseek rejected.",
      "SeLinOS ABI gateway: bounded volatile pread64 rejected.",
      "general mprotect enabled",
      "VFS cursor enabled",
      "network authority granted"
    ]
  },
  "not_claimed": [
    "general mprotect or W^X enforcement",
    "page permission transition, mremap, munmap or general virtual memory management",
    "stateful lseek cursor, nonzero seek offsets or general file positions",
    "pread64 on arbitrary FDs, arbitrary offsets, partial I/O or multi-page buffers",
    "TLS/ARCH_SET_FS, clone, blocking futexes, signals, ELF dynamic linking or general glibc ABI",
    "persistent filesystem, network stack, package retrieval, dpkg or apt"
  ]
}
